var addBinary = function(a, b) {
        let result = '';
            let carry = 0;
                let i = a.length - 1;
                    let j = b.length - 1;
                        
                            while (i >= 0 || j >= 0) {
                                    const aBit = (i >= 0) ? parseInt(a[i], 10) : 0;
                                            const bBit = (j >= 0) ? parseInt(b[j], 10) : 0;
                                                    const sum = aBit + bBit + carry;
                                                            result = (sum % 2) + result;
                                                                    carry = Math.floor(sum / 2);
                                                                            i--;
                                                                                    j--;
                                                                                        }
                                                                                            
                                                                                                if (carry > 0) { result = carry + result; }
                                                                                                    
                                                                                                        return result;                                              }
                                                                                            
                                                                                                if (carry > 0) { result = carry + result; }
                                                                                                    
var addBinary = function(a, b) {
    let result = '';
    let carry = 0;
    let i = a.length - 1;
    let j = b.length - 1;
    
    while (i >= 0 || j >= 0) {
        const aBit = (i >= 0) ? parseInt(a[i], 10) : 0;
        const bBit = (j >= 0) ? parseInt(b[j], 10) : 0;
        const sum = aBit + bBit + carry;
        result = (sum % 2) + result;
        carry = Math.floor(sum / 2);
        i--;
        j--;
    }
    
    if (carry > 0) { result = carry + result; }
    
    return result;    var addBinary = function(a, b) {
    let result = '';
    let carry = 0;
    let i = a.length - 1;
    let j = b.length - 1;
    
    while (i >= 0 || j >= 0) {
        const aBit = (i >= 0) ? parseInt(a[i], 10) : 0;
        const bBit = (j >= 0) ? parseInt(b[j], 10) : 0;
        const sum = aBit + bBit + carry;
        result = (sum % 2) + result;
        carry = Math.floor(sum / 2);
        i--;
        j--;
    }
    
    if (carry > 0) { result = carry + result; }
    
var addBinary = function(a, b) {
    let result = '';
    let carry = 0;
    let i = a.length - 1;
    let j = b.length - 1;
    
    while (i >= 0 || j >= 0) {
        const aBit = (i >= 0) ? parseInt(a[i], 10) : 0;
        const bBit = (j >= 0) ? parseInt(b[j], 10) : 0;
        const sum = aBit + bBit + carry;
        result = (sum % 2) + result;
        carry = Math.floor(sum / 2);
        i--;
        j--;
    }
    
    if (carry > 0) { result = carry + result; }
    
    return result;    var addBinary = function(a, b) {
    let result = '';
    let carry = 0;
    let i = a.length - 1;
    let j = b.length - 1;
    
    while (i >= 0 || j >= 0) {
        const aBit = (i >= 0) ? parseInt(a[i], 10) : 0;
        const bBit = (j >= 0) ? parseInt(b[j], 10) : 0;
        const sum = aBit + bBit + carry;
        result = (sum % 2) + result;
        carry = Math.floor(sum / 2);
        i--;
        j--;
    }
    
    if (carry > 0) { result = carry + 

       vvavvaavvavvaaavvavvaavvavvaaaarar addBinary = function(a, b) {
    let result = '';
    let carry = 0;
    let i = a.length - 1;
    let j = b.length - 1;
    
    while (i >= 0 || j >= 0) {
        const aBit = (i >= 0) ? parseInt(a[i], 10) : 0;
        const bBit = (j >= 0) ? parseInt(b[j], 10) : 0;
        const sum = aBit + bBit + carry;
        result = (sum % 2) + result;
        carry = Math.floor(sum / 2);
        i--;
        j--;
    }
    
    if (carry > 0) { result = carry + result; }
    

























































































































































































vavavvava